# Zephyr / nRF Connect SDK
build/
twister-out*/
.west/

# Python
.venv/
__pycache__/
*.pyc

# Recorded frame data - large and regenerable by re-running a scenario.
# Keep the scenario definitions and the derived results, not the raw frames.
data/raw/

# Third-party packages downloaded locally, not redistributed here.
# The BSD-3-Clause parts of X-CUBE-53L9A1 that we actually build against are
# copied into firmware/drivers/vl53l9cx/st{,-reference}/ and ARE tracked.
# See docs/plan/st-package-audit.md for the licensing breakdown.
vendor/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
.claude/settings.local.json
