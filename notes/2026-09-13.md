# 2026-09-13 — Planning: counting algorithms, detect stream, two images, plan cleanup

Planning only; no firmware or web changes.

## What was done
- Literature file for the counting algorithms (`docs/research/literature.md`).
- Victor's proposal evaluated: A1 background subtraction, A2 frame differencing, A3
  fusion, plus the mass-regression control and A4 plan view as a second representation
  (`docs/plan/detection-evaluation.md`).
- A3 promotion conflict written down (moved chair vs seated-at-power-up) — measured, not
  decided.
- Web interface plan for detection-mode selection, calibration panel and evaluation panel.
- Projection model closed as equal-angle; zone pitch corrected to 17.45 mrad.
- Two firmware images decided (Victor): evaluation image with the detect stream, then a
  separate energy-optimised image that only advertises the count.
- **`docs/plan/implementation.md` rewritten** as the single build order and consolidated
  specification. SUPERSEDED banners added to the overlapping sections in roadmap,
  people-counting, counting-algorithms, detection-evaluation and ble-streaming-and-web-ui.

## Found while consolidating
- The web recorder caps at `MAX_FRAMES = 4000` (~27 min at 2.5 fps) in browser memory:
  too short for the overnight empty-room recording and the 30-minute sit-down scenario.
  Now WP1.
- No `DECISIONS.md` entries had been written for 2026-09-12 or 2026-09-13; appended.

## Next
Bench B1/B2/B5 (Victor); software WP1 and WP2 can start without any bench result.
